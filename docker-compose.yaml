services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: treemapper
      POSTGRES_USER: treemapper
      POSTGRES_PASSWORD: treemapper
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
